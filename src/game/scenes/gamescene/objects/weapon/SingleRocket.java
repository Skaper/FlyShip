package game.scenes.gamescene.objects.weapon;

import engine.Scene;
import engine.gfx.Vector2;
import engine.objects.GameObject;

public class SingleRocket extends Rocket {
    public SingleRocket(Scene scene, Vector2 position, GameObject parent) {
        super(scene, position, parent);
    }
}
